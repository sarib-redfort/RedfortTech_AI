# Backend API base URL for authentication and CMS requests.
VITE_API_URL="http://localhost:5000/api/v1"

# Dummy-data toggle. "true" => use hardcoded demo data, ignore the backend.
# Keep this false so the CMS talks to the real NestJS backend.
VITE_USE_BACKEND="false"
