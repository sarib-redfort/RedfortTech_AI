// DEPRECATED - All text animations migrated to AOS
// Kept as empty file to prevent import errors during migration
// Remove after verifying AOS migration is complete

export function setupTextReveals() {
  // No-op: text now uses AOS via useAOS.ts
}

export function useRevealText() {
  // No-op: text now uses AOS via useAOS.ts
}